{
  "name": "fraud-link-detection",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm --prefix server install",
    "start": "npm --prefix server start"
  },
  "engines": {
    "node": ">=20"
  }
}